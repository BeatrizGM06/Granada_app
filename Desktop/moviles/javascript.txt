/*
You must be who Hermes mentioned
a monster here to block my way
I'd like to hurry up and end this
so if you don't have much to say
*/

let nombre = "Antonio" //No hace falta poner ';' y se pueden poner "" o ''
const edad = 19

//No poner 'var', siempre poner 'const' / 'let'

const repetidor = true
const altura = 1.78
// nombre = false <- EVITAR estas cosas

/*
Then oh I'll bring it on
I'm not dying here, I'm still fighting here
I'm holding on
'till I see through, 'till I've beaten you
*/

//Forma mala:
console.log("El alumno "+nombre+" tiene "+edad+" años")

/*
I already know your tactics
when you swallow you attack with
wirlpools that attract prey
but I see a disadvantage
'cause I don't even have to kill you
I just have to avoid you
see if you don't spit it out soon
all that water will destroy you
*/

//Forma correcta:
console.log(`El alumno ${nombre} tiene ${edad} años`) //Backtick ``

const alumno = {
  nombre: "Antonio",
  edad: 20,
  repetidor: true,
  direccion: {
    ciudad: "Granada",
    calle: "Albondón",
    numero: 5
  },
  telefono: {
    prefijo: "+666",
    numero: 66666666,
    tipo: "fijo"
  }
}

//Mostrar la edad del alumno:
console.log(alumno.edad)

//Mostrar por pantalla el prefijo del telefono:
console.log(alumno.telefono.prefijo)

//Hacer listas:

const lista = ["lunes", "martes", "viernes"]

console.log(lista[1])

//lista con 3 objetos alumno, cada alumno con nombre y curso
const lista = [
  {
    nombre: 'Pepe',
    curso: '1º dam'
  },
  {
    nombre: 'Luis',
    curso: '2º dam'
  },
  {
    nombre: 'Hermenegildo',
    curso: '2º dam'
  },
  {
    nombre: 'Eustakio',
    curso: '1º dam'
  }, //no da error al poner la última coma
]

//muestra en pantalla
//'el alumno hermenegildo estudia 2º dam'
console.log(`El alumno ${lista[2].nombre} estudia ${lista[2].curso}`)

for(let i=0;i<20;i++){
  console.log(i)
}

//2 for mejorados: "for in" / "for of"

const mensaje = "hola"

console.log(mensaje.toUpperCase())


//FUNCIONES ==============================================

//funcion que recibe dos numeros y devuelve la suma
function sumar(numero1,numero2){
  return numero1+numero2
}

console.log(sumar(1,1))



//funcion que calcula una potencia

function potencia(base,exponente){
  let resultado = 1
  for(i=0;i<exponente;i++){
    resultado = resultado*base;
  }
  return resultado
}

console.log(potencia(2,3))


const edad = 20

// crea una variable mayorEdad que valga "mayor" si edad >=18 y menor si edad<18

let mayorEdad = edad >= 18 ? "mayor" : "menor"
console.log(mayorEdad)


//////////////////////////////// - FUNCIONES -

function sumar(a,b){
  return a+b
}

console.log(sumar(3,5))

//////////////////////////////// - LAMBDAS - Una variable que guarda una función

const x = (a,b) => a+b //Lambda (arrow function)

console.log(x(2,3))


//Crea una variable/constante que guarde una funcion que reciba un numero y diga si es par

const par = a => a%2===0

//Usa la variable/constante anterior para ver si 24 es par

console.log(par(24))


//Crea una variable que guarde una funcion que reciba un mensaje y lo devuelva en mayusculas

let mayus = x => x.toUpperCase()

console.log(mayus("hola"))



//Crea una funcion que reciba un objeto alumno (nombre, edad, curso) y nos devuelva true si es mayor de edad

function mayorEdad(alumno){
  return alumno.edad>=18
}

//usa la funcion para ver si antonio de 50 años de 1dam es mayor de edad

const alumno={
  nombre:"Antonio",
  edad:50,
  curso: "1º dam"
}

console.log(mayorEdad(alumno))


//Crea una funcion que reciba un objeto alumno (nombre, edad, curso) y nos devuelva true si es mayor de edad

function mayorEdad(alumno){
  return alumno.edad>=18
}

//usa la funcion para ver si antonio de 50 años de 1dam es mayor de edad

console.log(mayorEdad({
  nombre:"Antonio",
  edad:50,
  curso: "1º dam"
}))